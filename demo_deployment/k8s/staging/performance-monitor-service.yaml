apiVersion: v1
kind: Service
metadata:
  labels:
    app: performance-monitor
    service-type: performance_monitor
  name: performance-monitor-service
  namespace: staging
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8082
  selector:
    app: performance-monitor
  type: ClusterIP
