# Node modules
node_modules/

# NEVER commit real env files — use .env.example instead
.env
.env.local
.env.development
.env.production
.env.staging
dashboard/.env
dashboard/.env.local
dashboard/.env.development
dashboard/.env.production
dashboard/.env.staging

# Secret replacement artifacts
expressions.txt

# CI/CD service account keys — never commit
*-key.json
*-cicd-key.json
gcp-cicd-key.json
firebase-cicd-key.json

__pycache__/
*.pyc
.venv/
.DS_Store

# Logs and cache
*.log
*.cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage/
*.tmp
*.temp
*.bak
*.orig

# Temporary Claude working directory files
tmpclaude-*-cwd
dashboard/tmpclaude-*-cwd

# Claude Code local session settings (machine-specific, not project config)
.claude/settings.json
.claude/settings.local.json

# Runtime databases (auto-generated)
*.db

# Export outputs
exports/

# Local tooling / build output
.mcp.json
dashboard/build/

# Extra Sensitive patterns
*.pem
*.key
*.p12
*.crt
# Block all .env variants except .env.example templates
.env*
!.env.example
!dashboard/.env.example
*secret*.yml
!.github/workflows/*secret*.yml
*secret*.json
*secret*.yaml
api_config.json


*.db

*.sqlite

nul


# Runtime deploy tracking — not source files
.last_good_deploy
deployments.log
